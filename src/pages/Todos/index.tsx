import React from "react";

interface TodosProps {}

const Todos = (props: TodosProps) => {
  //! State

  //! Function

  //! Render
  return <div>Todos generated!</div>;
};

export default React.memo(Todos);
